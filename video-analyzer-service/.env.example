# Supabase configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
# Optionnel: SUPABASE_KEY (identique)

# Flask config (si besoin d'exécuter api.py)
FLASK_HOST=0.0.0.0
FLASK_PORT=5000

# Scenedetect threshold (optionnel)
SCENEDETECT_THRESHOLD=27.0

# Temp directory (optionnel)
TEMP_DIR=./temp/videos
