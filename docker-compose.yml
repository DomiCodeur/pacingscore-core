services:
  # Backend Spring Boot API
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.backend
    ports:
      - "8080:8080"
    environment:
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_KEY=${SUPABASE_KEY}
      - TMDB_API_KEY=${TMDB_API_KEY}
      - YOUTUBE_API_KEY=${YOUTUBE_API_KEY}
      - ANALYZER_URL=http://analyzer:5000
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - app-network

  # Video Analyzer Worker (traite la file d'attente)
  analyzer:
    build:
      context: ./video-analyzer-service
      dockerfile: Dockerfile
    restart: always
    env_file:
      - ./video-analyzer-service/.env
    environment:
      - TEMP_DIR=/tmp/videos
    volumes:
      - ./video-analyzer-service/temp:/tmp/videos
    # Plus de port exposé : c'est un worker headless
    networks:
      - app-network

  # Frontend Angular
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.angular
    ports:
      - "9000:80"
    environment:
      - API_URL=http://backend:8080
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
